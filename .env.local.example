# Local dev OAuth credentials — copy to .env.local and fill in.
# The callback URL resolves automatically to http://localhost:5173/oauth-callback.html.
# Either add localhost to the production OAuth app's allowed redirect URIs,
# or use a separate dev app to keep dev tokens isolated from production.
VITE_GOOGLE_CLIENT_ID=82653401016-ef7iottrcbihi3eks96in6h8vsigu8lg.apps.googleusercontent.com
VITE_GOOGLE_CLIENT_SECRET=
VITE_MICROSOFT_CLIENT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
